<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE template-list SYSTEM "taglet_templates.dtd">

<!--
 #
 # See taglet_templates.dtd for valid tags and attributes
 #
 #
 # Each <template>-tag results in the creation of one particular template.
 # The value of the "name" attribute has to be unique among all templates.
 # It is used to reference the template in the taglet definition.
 #
 # The <headline>-tag specifies the HTML code for the term under which the
 # tag comment appears.
 # You may specify one parameter sign, i.e. #0.
 # You may use a single parameter multiple times,
 # i.e. <headline><![CDATA[bla #0 bla#0bla]]></headline> is valid.
 # It should contain unparsed character data, i.e. <![CDATA[....]]>
 #
 # The <content>-tag
 # The value of the "separator" attribute specifies if and how to break down
 # the comment into parameters. Possible values are:
 # - any single character:         at each occurance in the comment string
 # - the token "first-whitespace": breaks the comment at the first occurance of a whitespace
 # - the token "whitespace":       breaks the comment at each occurance of a whitespace
 # - the token "none" (default):   results in one token only, namely the whole comment
 #
 # The <content-item>-tag specifies the HTML code to wrap the tag comment in.
 # If multiple javadoc tag are present for a particular doc element,
 # then one content item is created for each tag comment.
 # You may specify up to ten parameter signs, i.e. #x, where -1<x<10.
 # You may use a single parameter multiple times,
 # i.e. <content-item><![CDATA[bla #0 bla#0bla]]></content-item> is valid.
 # It should contain unparsed character data, i.e. <![CDATA[....]]>
 # The <content-sep>-tag
 # Its value will be inserted between content items.
 # It should contain unparsed character data, i.e. <![CDATA[....]]>
 # It is optional and defaults to "".
 # The <content-start>-tag
 # Its value will be inserted directly after the headline,
 # before the first content item.
 # It should contain unparsed character data, i.e. <![CDATA[....]]>
 # It is optional and defaults to "".
 # The <content-end>-tag
 # Its value will be inserted directly after the the last content item.
 # It should contain unparsed character data, i.e. <![CDATA[....]]>
 # It is optional and defaults to "".
 #
 #
 # In general, it is a good idea to use the <DT>-tag for headlines and the
 # <DD>-tag for content, since all output generated by javadoc taglets
 # appears in definition lists.
 #
-->

<template-list>
  <!-- a template, that simulates the @param tag -->
  <template name="paramTemplate">
    <headline><![CDATA[<DT><B>#0:</B></DT>]]></headline>
    <content separator="first-whitespace">
      <content-item><![CDATA[<DD><code>#0</code> - #1</DD>]]></content-item>
    </content>
  </template>

  <!-- a very simple template -->
  <template name="simpleComment">
    <headline><![CDATA[<DT><B>#0:</B></DT>]]></headline>
    <content separator="none">
      <content-item><![CDATA[<DD>#0</DD>]]></content-item>
    </content>
  </template>

  <!-- a template, that simulates e.g. the @author tag -->
  <template name="simpleList">
    <headline><![CDATA[<DT><B>#0:</B></DT>]]></headline>
    <content separator="none">
      <content-sep><![CDATA[, ]]></content-sep>
      <content-start><![CDATA[<DD>]]></content-start>
      <content-item><![CDATA[#0]]></content-item>
      <content-end><![CDATA[</DD>]]></content-end>
    </content>
  </template>
</template-list>